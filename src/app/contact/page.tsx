import ContactPageClient from '@/components/layouts/ContactPageClient'

export const metadata = {
  title: 'contact',
  description: 'get in touch — email, github, linkedin',
}

export default function ContactPage() {
  return <ContactPageClient />
}
